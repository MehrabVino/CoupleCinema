{
  "name": "couple-cinema",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev -w apps/server\" \"npm run dev -w apps/web\"",
    "build": "npm run build -w apps/server && npm run build -w apps/web",
    "start": "concurrently \"npm run start -w apps/server\" \"npm run start -w apps/web\"",
    "db:generate": "npm run prisma:generate -w apps/server",
    "db:migrate": "npm run prisma:migrate -w apps/server"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
